/*
 * MainRoute Messages
 *
 * This contains all the text for the MainRoute container.
 */
import { defineMessages } from 'react-intl';
export const scope = 'src.containers.MainRoute';
export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MainRoute container!',
  },
});
