/*
 * ShopBrand Messages
 *
 * This contains all the text for the ShopBrand container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ShopBrand';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ShopBrand container!',
  },
});
