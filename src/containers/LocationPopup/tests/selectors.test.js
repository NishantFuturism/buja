// import { selectLocationPopupDomain } from '../selectors';
describe('selectLocationPopupDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
