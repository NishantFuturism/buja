/*
 * LocationPopup Messages
 *
 * This contains all the text for the LocationPopup container.
 */
import { defineMessages } from 'react-intl';
export const scope = 'app.containers.LocationPopup';
export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LocationPopup container!',
  },
});
