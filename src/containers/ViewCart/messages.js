/*
 * ViewCart Messages
 *
 * This contains all the text for the ViewCart container.
 */
import { defineMessages } from 'react-intl';
export const scope = 'app.containers.ViewCart';
export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ViewCart container!',
  },
});
