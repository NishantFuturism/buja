// import { selectRegisterOrignalDomain } from '../selectors';
describe('selectRegisterOrignalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
