/*
 * Wishlist Messages
 *
 * This contains all the text for the Wishlist container.
 */
import { defineMessages } from 'react-intl';
export const scope = 'app.containers.Wishlist';
export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Wishlist container!',
  },
});
