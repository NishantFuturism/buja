// import { selectDealdayProductDomain } from '../selectors';

describe('selectDealdayProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
