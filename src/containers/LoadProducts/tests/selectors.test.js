// import { selectLoadProductsDomain } from '../selectors';
describe('selectLoadProductsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
