/*
 * LoadProducts Messages
 *
 * This contains all the text for the LoadProducts container.
 */
import { defineMessages } from 'react-intl';
export const scope = 'app.containers.LoadProducts';
export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LoadProducts container!',
  },
});
