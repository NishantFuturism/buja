// import { selectRecentlyViewProductDomain } from '../selectors';

describe('selectRecentlyViewProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
