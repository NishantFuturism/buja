// import { selectSearchProductDomain } from '../selectors';
describe('selectSearchProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
