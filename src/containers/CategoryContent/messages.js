/*
 * CategoryContent Messages
 *
 * This contains all the text for the CategoryContent container.
 */
import { defineMessages } from 'react-intl';
export const scope = 'app.containers.CategoryContent';
export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CategoryContent container!',
  },
});
