// import { selectRelatedProductDomain } from '../selectors';
describe('selectRelatedProductDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
