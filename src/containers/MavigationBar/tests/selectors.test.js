// import { selectMavigationBarDomain } from '../selectors';

describe('selectMavigationBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
