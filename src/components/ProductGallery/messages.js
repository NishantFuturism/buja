/*
 * ProductGallery Messages
 *
 * This contains all the text for the ProductGallery component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProductGallery';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductGallery component!',
  },
});
