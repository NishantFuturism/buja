/*
 * EnterOtp Messages
 *
 * This contains all the text for the EnterOtp component.
 */
import { defineMessages } from 'react-intl';
export const scope = 'app.components.EnterOtp';
export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EnterOtp component!',
  },
});
